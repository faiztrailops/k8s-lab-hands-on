apiVersion: v1
kind: Pod
metadata:
  name: pod-secret-env

spec:
  containers:
  - name: app
    image: busybox
    command: ["sh","-c","echo $DB_USER && sleep 60"]
    env:
    - name: DB_USER
      valueFrom:
        secretKeyRef:
          name: db-credentials
          key: DB_USER
